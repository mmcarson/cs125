// Makes a pyramid of letters 
// By Marissa Carson

#include <iostream>
#include <string>

using namespace std;


int Characters(int);
string Spaces(int);
int LetterNumber(int);
char ToLetter(int);
void PrintChar(char);


int main ()
{
	int lineNumber = 0;
	string spaces;
	int characters;

	characters = Characters(lineNumber);
	spaces = Spaces(lineNumber);
	for(int charCount = 0; charCount >= characters; charCount++)
	int letterCode = LetterNumber(
	return 0;
}